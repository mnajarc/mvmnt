<%= simple_form_for(@tipo_persona) do |f| %>
    <%= f.input :tipo_persona %>
  <%= f.fields_for :caracter_tipo_personas do |builder| %>
  	<%= render 'layouts/caracteristica_fields', f: builder %>
  <% end %>
  <%= link_to_add_fields "Agrega caracteristica", f, :caracter_tipo_personas %>
    <br />
    <%= f.button :submit %>
<% end %>