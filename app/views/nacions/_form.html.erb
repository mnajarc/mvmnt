<%= simple_form_for(@nacion) do |f| %>
    <%= f.input :clave_pais %>
    <%= f.input :nombre %>
  <%= f.fields_for :estados do |builder| %>
  	<%= render 'estado_fields', f: builder %>
  <% end %>
  <%= link_to_add_field "Agrega estado", f, :estados %>
    <%= f.button :submit %>
<% end %>
